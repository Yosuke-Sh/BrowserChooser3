# BrowserChooser3 v0.1.5 リリースノート

## 🎉 新機能・改善

### 常駐（システムトレイ）モードの修正
- オプション画面の「常にトレイに常駐」設定（`AlwaysResidentInTray`）が実際に機能していなかった問題を修正し、閉じるボタン(×)でトレイに格納されるようにしました
- トレイの「終了」メニューからは従来通り完全に終了します

### 単一インスタンス化とURL引き渡し
- 2つ目のURLをクリックしたときに新しいプロセス・ウィンドウが増殖しないよう、既に起動中のインスタンスへURLを引き渡す仕組みを実装
- 常駐モードと組み合わせることで、2回目以降のブラウザ選択画面の表示が高速になります

### 起動処理の高速化
- ログ出力をバッチ化し、ログ1行ごとに発生していたファイルの開閉処理を削減
- ブラウザアイコンの取得結果をメモリ上にキャッシュし、同じアイコンの再抽出を回避

## 🐛 バグ修正

### トレイ格納中に勝手にブラウザが起動する問題
- システムトレイに格納されている間もカウントダウンタイマーが動き続け、時間経過で意図せずブラウザが起動してアプリごと終了してしまう問題を修正
- トレイ格納中はカウントダウンを停止し、復帰時に正しくリセットして再開するようにしました

## 🔧 技術的改善

### テストの追加
- 常駐モード（`OnFormClosing`の分岐、カウントダウン停止）に関するテストを追加
- 単一インスタンス化（`SingleInstanceManager`）に関するテストを追加
- ログのバッチ書き込み、アイコンキャッシュに関するテストを追加

## 📦 配布パッケージ

### インストーラー版
- ファイル名: `BrowserChooser3-Setup.exe`
- 設定: 管理者権限でのインストール、既定のブラウザ設定

## 🚀 インストール方法

### インストーラー版
1. `BrowserChooser3-Setup.exe`をダウンロード
2. 管理者権限で実行
3. インストールウィザードに従ってインストール
4. 設定ファイルは`%APPDATA%\BrowserChooser3`に保存
5. ログファイルは`%LOCALAPPDATA%\BrowserChooser3\Logs`に保存

## 📋 システム要件

- **OS**: Windows 10/11 x64
- **.NET**: .NET 8.0 Runtime
- **メモリ**: 512MB以上
- **ディスク**: 10MB以上の空き容量

## 🔄 アップデート方法

### v0.1.4からのアップデート
- 設定ファイルは自動的に保持されます
- 既にオプション画面で「常にトレイに常駐」を有効にしていた場合、アップデート後は実際にその通りに動作するようになります（今までは無効と同じ挙動でした）

## 📝 既知の問題

- なし

## 🙏 謝辞

このリリースに貢献してくださった開発者、テスター、ユーザーの皆様に感謝いたします。

## 📞 サポート

- **GitHub**: https://github.com/Yosuke-Sh/BrowserChooser3
- **Issues**: https://github.com/Yosuke-Sh/BrowserChooser3/issues
- **Wiki**: https://github.com/Yosuke-Sh/BrowserChooser3/wiki

---

**BrowserChooser3 v0.1.5** - 常駐モードの修正と単一インスタンス化による起動高速化

---
