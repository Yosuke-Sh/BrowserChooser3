# BrowserChooser3 v0.2.2 リリースノート

## ✨ 新機能・改善

### マルチモニター環境での起動位置に対応
- 選択画面が常にプライマリモニターの中央に表示され、URLをクリックした画面（マウスカーソルのある画面）に表示されない問題を修正しました
- 既定では、マウスカーソルがあるモニターのタスクバーを避けた中央に表示されるようになりました
- オプション画面の「表示」タブに「Startup Position」設定を追加し、従来どおり「プライマリ画面の中央」に固定することも可能です

## 🐛 バグ修正

### 既定のブラウザに設定済みでも警告ログが出る不具合を修正
- Windows 11で既定のブラウザとしてBrowserChooser3を設定しているにもかかわらず、起動のたびに「BrowserChooser3が既定のブラウザとして設定されていません」という警告がログに記録される不具合を修正しました
- 原因は、既定ブラウザの判定処理がWindows 8以前の仕組み（`HKCR\http\shell\open\command`）のみを参照しており、Windows 11以降で実際に既定を管理する`UserChoice`レジストリキーを見ていなかったことによるものです
- 判定処理を`UserChoice`ベースに修正し、HTTP/HTTPSの両方が正しく検出されるようにしました
- あわせて、Windows 11では効果のなかった「初回起動時の既定ブラウザ自動設定」処理を廃止し、初回起動時にWindowsの既定アプリ設定画面（設定 > アプリ > 既定のアプリ）を案内する方式に変更しました
- 既定に設定していない場合のログレベルもWARNINGからINFOに変更しました（異常ではなくユーザーの選択であるため）

## ⚠️ 互換性について

- 破壊的変更はありません
- 設定ファイル（`BrowserChooser3Config.xml`）の後方互換性は維持されており、既存の設定はそのまま引き継がれます。新設定項目（起動位置）が存在しない既存ファイルは、既定値（マウスカーソルのある画面の中央）で動作します
- UI/UXの既存レイアウト・色・フォント・間隔の変更はありません（オプション画面の「表示」タブに設定項目を1つ追加）

## 📝 既知の問題

- 混在DPI環境（モニターごとにスケーリング倍率が異なる構成）では、副モニターに表示した際の見た目が最適化されない場合があります（アプリのDPIモードが`SystemAware`のため）

## 📦 配布パッケージ

### インストーラー版
- ファイル名: `BrowserChooser3-Setup.exe`
- 設定: 管理者権限でのインストール、既定のブラウザ設定
- .NET 10 Desktop Runtime が未導入の場合は自動導入

## 🚀 インストール方法

### インストーラー版
1. `BrowserChooser3-Setup.exe`をダウンロード
2. 管理者権限で実行
3. .NET 10 Desktop Runtime が未導入の場合、インストーラーが自動的にダウンロード・インストールします
4. インストールウィザードに従ってインストール
5. 設定ファイルは`%APPDATA%\BrowserChooser3`に保存
6. ログファイルは`%LOCALAPPDATA%\BrowserChooser3\Logs`に保存

## 📋 システム要件

- **OS**: Windows 10/11 x64
- **.NET**: .NET 10.0 Desktop Runtime（未導入の場合はインストーラーが自動導入）
- **メモリ**: 512MB以上
- **ディスク**: 10MB以上の空き容量

## 🔄 アップデート方法

### v0.2.1からのアップデート
- 設定ファイルは自動的に保持されます
- 追加の手動作業は不要です

## 🙏 謝辞

このリリースに貢献してくださった開発者、テスター、ユーザーの皆様に感謝いたします。

## 📞 サポート

- **GitHub**: https://github.com/Yosuke-Sh/BrowserChooser3
- **Issues**: https://github.com/Yosuke-Sh/BrowserChooser3/issues
- **Wiki**: https://github.com/Yosuke-Sh/BrowserChooser3/wiki

---

**BrowserChooser3 v0.2.2** - マルチモニター起動位置対応と既定ブラウザ判定の修正

---
